[project]
name = "renovate-python-optionals"
version = "0.0.0"

[project.optional-dependencies]
test = [
    "pytest ~=8.4.1",
]
docs = [
    "sphinx == 8.2.2",
]

[dependency-groups]
dev = [
    "pre-commit == 4.2.0",
]
